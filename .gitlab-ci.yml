pages:
  stage: deploy
  script:
    - mkdir public
    - cp index.html public/
    - cp LICENSE public/ || true
    - cp README.md public/ || true
  artifacts:
    paths:
      - public
  only:
    - main
